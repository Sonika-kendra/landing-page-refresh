interface RegistrationModalProps {
  isOpen: boolean;
  onClose: () => void;
}

const RegistrationModal = (_props: RegistrationModalProps) => {
  // Registration modal is temporarily disabled across the app.
  return null;
};

export default RegistrationModal;
